{
  "name": "env-action",
  "version": "1.0.0",
  "description": "Github Action to create .env using secrets.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test && git add . && git commit -m 'update' && git push origin"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/eteg/env-action.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/eteg/env-action/issues"
  },
  "homepage": "https://github.com/eteg/env-action/blob/master/README.md",
  "dependencies": {
    "@actions/core": "^1.2.5",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.0.3",
    "shelljs": "^0.8.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0",
    "eslint": "^8.18.0",
    "jest": "^28.1.1"
  }
}
